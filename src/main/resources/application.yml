spring:
  application:
    name: olympic

  datasource:
    url: jdbc:mysql://localhost:3306/olympic_db?useSSL=false&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false
    encoding: UTF-8

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 6MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: olympic946@gmail.com
    password: khmxyvekbgceliae
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

# ── Configuración propia de OLYMPIC ─────────────────────────
app:
  upload:
    dir: src/main/resources/static/imagenes/productos
    max-size-bytes: 5242880 # 5 MB
    allowed-extensions: jpg,jpeg,png,webp

  recuperacion-password:
    expiracion-minutos: 15
    remitente: olympic946@gmail.com